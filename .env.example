# Copy this file to `.env.local` and fill in values, or set variables in Vercel
# (Project → Settings → Environment Variables) for Production / Preview / Development.

# -----------------------------------------------------------------------------
# Required — MongoDB Atlas (or any MongoDB deployment)
# Include database name in the path, e.g. /your_db_name
# Atlas example:
#   mongodb+srv://USER:PASS@cluster.xxxxx.mongodb.net/dr_sunita_db?retryWrites=true&w=majority&appName=app
# -----------------------------------------------------------------------------
MONGODB_URI=
